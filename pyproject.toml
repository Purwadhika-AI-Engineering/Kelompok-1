[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "olist-insight-assistant"
version = "0.1.0"

[tool.setuptools]
py-modules = ["config"]

[tool.setuptools.packages.find]
include = ["agent*", "api*", "app*", "services*", "observability*", "scripts*"]